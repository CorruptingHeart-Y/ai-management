# US01 实际验证记录

负责人：彭佳成。执行日期：2026-09-20。基线：`6906e49`。分支：`feature/us01-members`。以下是本次 AI 自动验证，不代表人工 diff 审查、课堂会议或团队 DoD 验收。

## 环境和全量回归

Windows、Node.js v26.1.0、npm 11.13.0。按仓库锁文件执行 `npm ci`。既有代码修改前 `npm test` 为 12/12 通过。

2026-09-20 21:04:12 +08:00 执行：

```powershell
node --test --test-reporter=tap test/members.test.js test/tasks.test.js test/task-status.test.js
```

结果：24/24 通过、0 失败（19 个顶层测试含 5 个子测试；US01 15 项、原 US04 5 项、原 US05 4 项）。[原始 TAP 日志](us01-tests.tap) 保留实际输出。所有 API 测试使用独立临时数据，原 US04/US05 测试文件和版本库 `data/db.json` 未改动。

## 浏览器检查

2026-09-20 21:03:00 +08:00，用 Playwright 驱动本机无头 Google Chrome，使用临时 JSON 和临时监听端口，8/8 场景通过。[原始 JSON 结果](us01-browser-results.json) 中时间为 UTC。

| 场景 | 结果 |
|---|---|
| 四名成员姓名、角色与实验一一致，负责人选项与 API 一致 | 通过 |
| 选择彭佳成创建任务，刷新后负责人保持，切换到 DOING/DONE 再刷新 | 通过 |
| 390px 手机视口，成员与表单不横向溢出 | 通过 |
| 空成员列表显示说明并禁用创建、分配 | 通过 |
| 成员接口 HTTP 500 时提示重试，恢复后重新展示 | 通过 |
| 成员接口网络失败时可重试恢复 | 通过 |
| 任务接口 HTTP 500 不清除正常的成员列表与选项 | 通过 |
| 姓名与分工含 HTML 时按文本展示，不产生注入节点 | 通过 |

页面未出现未捕获脚本错误。检查了 [桌面截图](us01-members.png) 和 [手机截图](us01-members-mobile.png)，姓名、分工及任务内容均清晰完整。截图中的任务来自隔离演示数据，不写入团队数据文件。

浏览器验证脚本保存在本次工作区 `lab2/.us01-qa/browser-check.cjs`，其临时依赖未写入项目 package.json。项目必需的自动验收测试随仓库 `test/members.test.js` 提交，无需安装浏览器依赖即可运行 `npm test`。人工演示步骤见 [US01 接口说明](../us01-members.md#评审演示步骤)。

## AI 辅助审查

独立审查智能体检查了与同学基线的差异：读取成员不覆盖任务，接口格式兼容，列表和选项共用响应，成员文本安全渲染，空列表和错误恢复可用。未发现阻断 US01 的问题。该结论不能勾选“人工 diff 审查”。

仍待彭佳成及 DRI 进行人工 diff 审查，并在合入 develop 后验证和确认 DoD；当前分支自动验证不替代这些步骤。
